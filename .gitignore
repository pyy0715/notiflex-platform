# Go build output
/app/notiflex-api
*.exe
*.test

# OS / editor
.DS_Store
.idea/
.vscode/

# Terraform (local state only — not remote-backed)
terraform/**/.terraform/
terraform/**/*.tfstate
terraform/**/*.tfstate.*
terraform/**/.terraform.lock.hcl
terraform/**/crash.log

# kustomize --enable-helm local chart cache (not used in this repo's ArgoCD flow)
k8s/**/charts/
